#include <iostream>
#include <string>
using namespace std;

int main(){
	int n1, n2;
  while(cin >> n1){
    cin >> n2;
    int a[n1], b[n2];
    int height[n1 + 1][n2 + 1] = {};
    for(int i = 0; i <= n1; i++){
       cin >> a[i];
    }
    for(int i = 0; i <= n2; i++){
       cin >> b[i];
    }
    for(int i = 0; i <= n1; i++){
      for(int j = 0; j <= n2; j++){
        if(a[i] == b[j]){
          height[i + 1][j + 1] = height[i][j] + 1;
        }else{
        
      }
    }
  }
	return 0;
} 
